# Npm package boilerplate

Use this to bootstrap your npm package development.

Run `npm run dev` to auto build your files on save.

Run `npm publish --access public` to publish your package to the `@lefapps` org on the npm registry.